package ls.yylx.lscodestore.algorithms

fun findRedundantDirectedConnection(edges: Array<IntArray>): IntArray {
    val hash = hashMapOf<Int, HashSet<Int>>()
    edges.forEachIndexed { index, ints ->
        if (hash.containsKey(ints[0])){

        }
    }

}